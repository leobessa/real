require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe Asset do
  it "should increase the amount if account is debited"
  it "should decrease the amount if account is credited"
end
